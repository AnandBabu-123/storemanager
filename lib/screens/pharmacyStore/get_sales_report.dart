import 'package:flutter/material.dart';

class GetSalesReport extends StatelessWidget {
  const GetSalesReport({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Sales Report"),
      ),
    );
  }
}
